const Action = require("../Actions/Action.cjs");

module.exports = class AddGrass extends Action {
  constructor(mapOfTheGame) {
    super(mapOfTheGame);
  }

  produce() {
    //     const mapOfTheGame = this.map.map;
    //     const requiredCount = Math.floor(mapOfTheGame.length / 5);
    //     const currentCount = 0;
    //     for (let i = 0; i < mapOfTheGame.length; i++) {
    // 	}
  }
};
